generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Mood {
  id                  String   @id @default(cuid())
  text                String
  rating              Int
  score               Int
  placeName           String
  latitude            Float?
  longitude           Float?
  weatherSummary      String?
  weatherIcon         String?
  imageUrl            String?
  textSentimentScore  Float?
  imageSentimentScore Float?
  createdAt           DateTime @default(now())
}
